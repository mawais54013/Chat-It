/**
 * Test for rescue.
 * Runs with mocha.
 */
'use strict'

const rescue = require('../lib/rescue')
const { ok, equal } = require('assert')

describe('rescue', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
