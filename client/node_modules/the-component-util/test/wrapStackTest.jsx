/**
 * Test for wrapStack.
 * Runs with mocha.
 */
'use strict'

import wrapStack from '../lib/wrapStack'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from 'the-script-test'

describe('wrap-stack', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    wrapStack(
      {name: 'A'},
      {name: 'B', wrapStack: ['A']},
      {name: 'B', wrapStack: ['A,B,C']},
    )
  })
})

/* global describe, before, after, it */
