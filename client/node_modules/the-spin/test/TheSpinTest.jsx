/**
 * Test for TheSpin.
 * Runs with mocha.
 */
'use strict'

import TheSpin from '../lib/TheSpin'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from 'the-script-test'

describe('the-spin', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = render(
       <TheSpin />
    )
    ok(element)
  })
})

/* global describe, before, after, it */
