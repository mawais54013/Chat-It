/**
 * Test for asHOC.
 * Runs with mocha.
 */
'use strict'

import asHOC from '../lib/asHOC'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from 'the-script-test'

describe('as-ho-c', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    ok(asHOC)
  })
})

/* global describe, before, after, it */
