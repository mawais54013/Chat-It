/**
 * Test for TheTalk.
 * Runs with mocha.
 */
'use strict'

import '@babel/polyfill'
import TheTalk from '../lib/TheTalk'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from 'the-script-test'

describe('the-talk', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = render(
      <TheTalk/>
    )
    ok(element)
  })
})

/* global describe, before, after, it */
