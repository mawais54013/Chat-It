/**
 * Test for canTouch.
 * Runs with mocha.
 */
'use strict'

const canTouch = require('../lib/canTouch')
const { ok, equal } = require('assert')

describe('can-touch', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
