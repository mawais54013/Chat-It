/**
 * Test for isBrowser.
 * Runs with mocha.
 */
'use strict'

const isBrowser = require('../lib/isBrowser')
const { ok, equal } = require('assert')

describe('is-browser', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(!isBrowser())
  })
})

/* global describe, before, after, it */
