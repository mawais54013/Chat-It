/**
 * Test for getUserAgent.
 * Runs with mocha.
 */
'use strict'

const getUserAgent = require('../lib/helpers/getUserAgent')
const { ok, equal, deepEqual } = require('assert').strict

describe('get-user-agent', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
