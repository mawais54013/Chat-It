/**
 * Test for set.
 * Runs with mocha.
 */
'use strict'

const set = require('../lib/set')
const { ok, equal } = require('assert')

describe('set', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
