/**
 * Test for get.
 * Runs with mocha.
 */
'use strict'

const get = require('../lib/get')
const { ok, equal } = require('assert')

describe('get', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {
    ok(!get('window'))
  })
})

/* global describe, before, after, it */
