/**
 * Test for setupVideoElm.
 * Runs with mocha.
 */
'use strict'

import setupVideoElm from '../lib/helpers/setupVideoElm'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from 'the-script-test'

describe('setup-video-elm', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    ok(setupVideoElm)
  })
})

/* global describe, before, after, it */
