/**
 * Test for show.
 * Runs with mocha.
 */
'use strict'

const show = require('../lib/show')
const { ok, equal } = require('assert')

describe('show', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
