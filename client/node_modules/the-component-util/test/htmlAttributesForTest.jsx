/**
 * Test for htmlAttributesFor.
 * Runs with mocha.
 */
'use strict'

import htmlAttributesFor from '../lib/htmlAttributesFor'
import React from 'react'
import { ok, deepEqual } from 'assert'

describe('html-attributes-for', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    const attributes = htmlAttributesFor({
      foo: 'This is foo',
      title: 'The title',
      'data-hoge': 'hoge'
    })
    deepEqual(attributes, {title: 'The title', 'data-hoge': 'hoge'})
  })
})

/* global describe, before, after, it */
