/**
 * Test for once.
 * Runs with mocha.
 */
'use strict'

const once = require('../lib/once')
const { ok, equal } = require('assert')

describe('once', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
