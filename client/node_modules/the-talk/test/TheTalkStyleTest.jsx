/**
 * Test for TheTalkStyle.
 * Runs with mocha.
 */
'use strict'

import TheTalkStyle from '../lib/TheTalkStyle'
import React from 'react'
import { ok, equal } from 'assert'
import { render } from 'the-script-test'

describe('the-talk-style', () => {
  before(() => {
  })

  after(() => {
  })

  it('Render a component', () => {
    let element = render(
       <TheTalkStyle />
    )
    ok(element)
  })
})

/* global describe, before, after, it */
