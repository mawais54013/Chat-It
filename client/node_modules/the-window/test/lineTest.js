/**
 * Test for line.
 * Runs with mocha.
 */
'use strict'

const line = require('../lib/line')
const { ok, equal } = require('assert')

describe('line', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
